# Shared intake

Use this folder for reusable assets that do not belong to one project yet: shared diagrams, palettes, brand references, icons or cross-section notes.

Default status for new uploads: `needs-review`.

If a shared asset becomes project-specific, document the relationship in `../MANIFEST.md` instead of moving it during a docs-only pass.
